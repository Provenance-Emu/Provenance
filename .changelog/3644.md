### Fixed
- **Insert Coin tile unresponsive in MAME/CPS1/CPS2/CPS3** — the pause menu fully paused the core, so the coin press/release both landed while `retro_run()` was never executing and the libretro core never sampled it. The tile now briefly resumes emulation to let the press register, then re-pauses.
