### Fixed
- **JIT Indicator Tap Presentation** — replaced full-screen cover sheet with a compact `UIAlertController` banner showing a brief status message when the user taps the JIT HUD pill.
- **JIT Indicator Interaction** — enabled user interaction on the overlay so the pill button is actually tappable (previously blocked by `isUserInteractionEnabled = false`).
- **JIT Status Messages** — updated indicator messages to plain, user-friendly copy: "JIT compilation active — best performance enabled", "JIT unavailable — some cores may run slower or be unstable", and a JIT-required guidance message.

### Changed
- **JIT Core Detection** — extracted the hardcoded JIT core keyword list into a new `JITCoreCapability` enum in `PVUIBase`, making it the single source of truth for JIT-relevant core identification and easier to extend when the JIT Capability Matrix (#2793) lands.
