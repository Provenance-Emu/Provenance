### Added
- **Document existing N64 RetroArch netplay support** — Clarify that `mupen64plus_next` and `parallel_n64` RetroArch cores already support network play today via the existing `PVRetroArchCoreBridge+PVNetplayCapable` implementation (HAVE_NETPLAY already enabled), and document how to access "Network Play" from the pause menu when using either RA N64 core.
- **MupenGameCore+PVNetplayCapable stub** — Native Mupen64Plus (`MupenGameCore`) now explicitly adopts `PVNetplayCapable` with `supportsNetplay = false`, documenting the pathway for future mupen64plus-netplay plugin integration (Option A rollback netplay).
