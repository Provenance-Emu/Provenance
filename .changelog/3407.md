### Added
- **ROM Drag Export** — Long-press a game tile in the library grid and drag it to Files.app, AirDrop, or any compatible target to export the ROM file; unsupported on tvOS. For iCloud-evicted ROMs, drag returns an empty provider and triggers a background re-download so the next attempt succeeds
