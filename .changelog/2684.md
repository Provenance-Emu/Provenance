### Added
- **AirPlay in Pause Menu** — Added AirPlay route picker to both the tile-based pause menu (as an "AirPlay" tile in the GAME section) and the classic RetroMenuView (as a styled row in the Options tab), allowing users to stream audio/video to AirPlay devices without leaving a game session.
