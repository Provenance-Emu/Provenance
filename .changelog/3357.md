### Fixed
- **RetroArch CoreMIDI defaults** — `midi_input` and `midi_output` now default to `"coremidi"` instead of `"OFF"`, so DOSBox-Pure and other MIDI-capable RetroArch cores produce MIDI output on launch without any manual configuration.
