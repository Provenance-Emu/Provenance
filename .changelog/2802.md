### Added
- **PVToast** — New in-game toast notification overlay (`PVToastManager`, `PVToastView`, `PVToastHostingController`) with retrowave aesthetics, queue-based stacking, auto-dismiss, persistent toasts, and VoiceOver support.
