### Added
- **NetplayJoinConfirmView** — ROM hash verification screen shown before joining a netplay room; displays green/orange match status, room info, truncated hash comparison, and a mismatch warning with context

### Changed
- **NetplayRoomBrowserView** — player joins now route through `NetplayJoinConfirmView` for hash confirmation before connecting; spectate taps bypass the confirm screen as ROM version is irrelevant for spectators
