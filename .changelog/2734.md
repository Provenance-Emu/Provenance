### Added
- **Per-Game Core Options in Game Info Page** — Added a "Core Options" section to the Game Detail / Info page that lets users view and edit per-game emulator settings. The section shows a live override count badge when game-specific settings are active, and displays a descriptive disabled state for unmatched ROMs or cores without configurable options.
