### Added
- **Light Gun Input Primitives** — adds `LightGunResponder` protocol, `GCMouseLightGunDriver` (bridges `GCMouse` delta events to normalised light-gun aim/fire calls), and `LightGunLifecycleManager` (attach/detach helper for cores that support light guns).
