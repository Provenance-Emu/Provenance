### Added
- **RetroArch Netplay Integration** — `PVRetroArchCoreBridge` now conforms to `PVNetplayCapable`, enabling `PVNetplayManager` to host, join, and stop RetroArch netplay sessions natively. `PVEmulatorViewController` automatically registers the active core as the netplay bridge on emulation start and deregisters it on stop
