### Added
- **ColecoVision Companion Controller wiring** — `PVGearcolecoCore` now conforms to `CompanionControllerCapable`, mapping the 12-key numpad, two side action buttons, and D-pad from the Companion overlay to `PVColecoVisionButton` events in the Gearcoleco core
- **CompanionButton bitmask tests** — unit tests asserting each `CompanionButton` rawValue is a non-zero single-bit flag, preventing accidental DSU on-the-wire collisions
