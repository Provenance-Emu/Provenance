### Fixed
- **Screen Filter JSON Decoding** — `DeltaSkinScreen` now correctly decodes `filters` from skin `info.json` and constructs `CIFilter` instances; CRT, scanline, sepia, and blur filters specified in skins now apply at runtime. Also adds `filterInfos` for round-trip encoding fidelity and wires decoded filters into the Metal rendering pipeline via `PVEmulatorViewController`.
