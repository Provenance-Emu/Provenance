### Added
- **YAML Core Manifest** — Replace 4 manually-maintained urls.txt + xcfilelist files with a single `cores.yml` manifest and a Python generator script (`Scripts/generate_core_lists.py`).
- **CoreManager Swift CLI** — New `Scripts/CoreManager/` Swift package providing `generate`, `validate`, `diff`, and `bootstrap` subcommands for managing libretro buildbot cores.
