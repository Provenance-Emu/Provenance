### Fixed
- **Pause menu button layout stability** — CORE category tab and save-state action buttons are now always rendered at fixed positions; unsupported items are dimmed in-place instead of removed, preventing sibling items from shifting and breaking muscle memory
