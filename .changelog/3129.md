### Added
- **Mupen64Plus Auto Pak Detection** — Controller pak type is now auto-detected from the bundled `mupen64plus.ini` ROM database (`Mempak`/`Rumble` flags) after ROM load, mirroring RetroArch mupen64plus-next behaviour. New default is "Auto (ROM Database)" — legacy users with an explicit pak setting are unaffected.
