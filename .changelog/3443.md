### Fixed
- **ROM File Provider — Copilot review fixes** — Addressed all Copilot review issues: replaced `NSCocoaErrorDomain/NSFeatureUnsupportedError` with `NSFileProviderError(.unsupported)` in mutation methods; added filename sanitization to strip `/` and `:` from system names and game titles; removed `.allowsEvicting` capability since there is no rehydration path; tightened platform guard to `#if canImport(FileProvider) && (os(iOS) || os(macOS) || os(visionOS))`.
