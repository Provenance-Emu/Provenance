### Added
- **Licenses UI Rewrite** — LicensesView completely rewritten as native SwiftUI with a retro-styled card list, search/filter bar (iOS/macOS), and in-app Safari viewer for project URLs (iOS/macOS Catalyst). tvOS opens URLs in the system browser. License grouping is scaffolded for future use pending `PVCore.license` support (#3236).
