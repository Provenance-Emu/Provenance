### Added
- **Licenses UI Rewrite** — LicensesView rewritten as native SwiftUI with a retro-styled card list. Search/filter bar on iOS and macOS Catalyst; tvOS opens URLs in the system browser; iOS/Catalyst show an in-app Safari sheet; native macOS uses the system openURL handler. License grouping is scaffolded (all cores show "TBD" pending `PVCore.license` support in #3236). Core data is loaded asynchronously on first appear via `.task` to avoid blocking SwiftUI init.
