### Changed
- **ArtworkMatchingService** — Extracted shared artwork search fallback logic into a new `ArtworkMatchingService` actor (`PVLibrary`) used by both the import pipeline (`ArtworkSearchQueue`) and the manual batch artwork matching UI (`BatchArtworkMatchingView`), eliminating duplicated code and keeping progressive fallback behaviour consistent across the app
