### Changed
- **ArtworkMatchingService** — Extracted shared artwork search fallback logic into a new actor used by both the import pipeline and the manual batch artwork matching UI, reducing duplicate code and keeping fallback behaviour consistent across the app
