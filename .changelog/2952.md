### Added
- **Save State Core Version Tracking** — `createdWithCoreVersion` and `coreIdentifier` now flow from Realm/SwiftData through `SaveStateRowViewModel`, `RetroSaveStateItem`, `RealmSaveStateDriver`, and `RetroSaveStatesStore.mapSaveState`; `PVPrimitives.SaveState` and `SaveStateInfoProvider` also carry `createdWithCoreVersion` for JSON/CloudKit parity; mock driver updated with sample values for preview/testing.
