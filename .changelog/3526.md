### Added
- **ArtworkMatchingService** — fast exact-match artwork lookup at ROM import time with a ≤2 s timeout, giving newly imported games artwork immediately before falling back to `ArtworkSearchQueue`
