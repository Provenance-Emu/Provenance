### Added
- **ArtworkMatchingService** — fast, exact-match multi-source artwork lookup at ROM import time that runs with a ≤2 second timeout before falling back to the existing `ArtworkSearchQueue` background search, giving newly imported games artwork immediately when an exact match is found
