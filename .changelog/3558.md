### Changed
- **Vulkan double-buffering** — Replace `vkQueueWaitIdle` (full-queue stall) with per-frame `VkFence` synchronisation in the thin libretro Vulkan bridge. `get_sync_index_mask` now returns 3 (double-buffer), narrowing GPU/CPU synchronisation scope and signalling double-buffer capability to cores.
