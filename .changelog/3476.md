### Added
- **PatchableCore for mGBA** — Declares `PVmGBACore` conformance to `PatchableCore` with supported formats `[.ips, .ips32, .ups]`, exposing mGBA's built-in patch routines to the protocol layer
