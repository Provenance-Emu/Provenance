### Added
- **SideKit JIT for AltStore/SideStore** — Enabled `_USE_ALTKIT` compilation flag for iOS in PVJIT/JITManager, activating `ServerManager`-based JIT acquisition via AltStore and SideStore.

### Fixed
- **PVJIT tvOS build** — Made SideKit dependency in the PVJIT target conditional on iOS (matching JITManager), preventing link errors on tvOS where SideKit is unavailable.
