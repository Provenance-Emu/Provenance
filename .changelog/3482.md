### Added
- **DOSBox Folder ROM Support** — Game folders containing `.conf`, `.exe`, `.bat`, or `.com` files at the root level are now recognised as DOSBox ROMs. Dropping such a folder into the Imports directory automatically imports it as a single DOS game entry, and DOSBox-Pure receives the folder path directly — no zipping required. `.dosz` support is unchanged
