### Added
- **TIC-80 System** — Added full system definition to `systems.plist` with D-pad, A/B/X/Y, L/R shoulders, Start/Select controls and `.tic`/`.zip` extension support.
- **TIC-80 RetroArch Core entry** — Added `tic80.libretro.framework` core descriptor in `Core.plist` mapping to `com.provenance.tic80` system; keyboard input already wired.
- **TIC-80 cores.yml entry** — Added (disabled) `tic80` entry in `cores.yml` for buildbot tracking; enable when the core appears on the libretro buildbot.
