### Fixed
- **Recent Saves autosave flooding** — The "Recent Saves" section in the Game Library now shows at most one autosave per game (the most recent), preventing timed/session autosaves from flooding the strip and hiding saves from other games. Manual saves are always shown. The full Save State Management UI remains unfiltered.
