### Fixed
- **Duplicate Localization Key** — Removed duplicate `"Unknown"` key that appeared twice in all `Strings.strings` files (en, es, it, ja, nl, pt-BR, ru).
- **Untranslated Language Copies** — Marked Italian (it) and Japanese (ja) `Strings.strings` files with TODO comments indicating they are English copies awaiting real translation.
- **Incomplete Simplified Chinese** — Expanded `zh-Hans.lproj/Strings.strings` to match the full key set from `en.lproj/Strings.strings`; the existing `加载中` translation is preserved and remaining keys are English stubs pending translation.
