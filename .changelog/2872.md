### Fixed
- **Duplicate Localization Key** — Removed duplicate `"Unknown"` key that appeared twice in all `Strings.strings` files (en, es, nl, pt-BR, ru).
- **Untranslated Language Copies** — Marked Italian (it) and Japanese (ja) `Strings.strings` files with TODO comments indicating they are English copies awaiting real translation.
- **Incomplete Simplified Chinese** — Expanded `zh-Hans.lproj/Strings.strings` from 1 key to all 77 keys; the single existing translation (`加载中`) is preserved and remaining keys are English stubs pending translation.
