### Added
- **Korean (ko) and Arabic (ar) translations** — Add AI-generated translations based on community contributions from issues #2255 and #2256, adding `Strings.strings` and `Localizable.strings` to the respective lproj directories and registering both languages via `CFBundleLocalizations` in Info.plist files.
