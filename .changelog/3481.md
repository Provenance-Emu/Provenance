### Added
- **Pause Menu parity** — new Shader Settings, Port Devices, MIDI Device, and Skins tiles in `PauseTileMenuView` to match `RetroMenuView` feature set

### Fixed
- **Pause Menu indicator overlay** — `showSkinOptions()` now hides the status indicator overlay before presenting and restores it on dismiss, matching `showMenu`'s lifecycle
- **Keyboard/mouse overlay toggles** — no longer leave the game paused after toggling overlays from the tile pause menu
- **Quit/Resume ordering** — tile menu now matches RetroMenuView: Resume → Save & Quit → Quit ordering
