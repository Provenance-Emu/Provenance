### Fixed
- **SGFX Input Routing** — SuperGrafx (`.SGFX`) controller input now correctly routes through `PVPCESystemResponderClient` / `PVPCEButton` instead of the NEC PC-FX handler, fixing broken button input for SuperGrafx games.
