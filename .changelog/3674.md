### Fixed
- **Supported-systems documentation** — README no longer credits Apple II to the Atari800 core (it's MAME) and now flags it as absent from the App Store build; added the missing Atari 8-bit (400/800/XL/XE) entry; corrected the Sony and Atari section counts; clarified that PS2 ships in the App Store binary but only runs with JIT.
- **Stale `systems.plist` metadata** — nine systems (PSP, GameCube, Dreamcast, DS, 3DS, Atari ST, DOS, MSX, MSX2) had a stale `PVSupported=false` flag that fed the wiki's auto-generated System Reference page but wasn't read anywhere in the app itself, causing shipped systems to be misreported as "in development."
