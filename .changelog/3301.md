### Fixed
- **HW-render cores (GLES3) now display frames via thin libretro wrapper** — `rendersToOpenGL` now returns `YES` for hardware-render cores; the FBO color texture is bound to the shared `IOSurface` from the Metal view's render delegate; and `RETRO_HW_FRAME_BUFFER_VALID` frames now trigger `didRenderFrameOnAlternateThread` so the Metal blit fires each frame. Fixes blank display for mupen64plus paraLLEl-RDP, flycast, Beetle PSX HW (GLES), and similar cores
