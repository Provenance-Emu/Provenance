### Added
- **Localization Audit Script** — `Scripts/audit_localization.sh` enumerates all `Text("…")` and `NSLocalizedString` calls, cross-references existing `.strings` files, and reports coverage gaps and missing keys per language
