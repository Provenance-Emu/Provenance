# For more information about this configuration visit
# https://docs.fastlane.tools/actions/gym/#gymfile

# In general, you can use the options available
# fastlane gym --help

#scheme("Provenance") # ie target
workspace("Provenance.xcworkspace")
include_bitcode(false)
#clean(false)

# sdk("iphoneos9.0")

#build_path(...) The directory in which the archive should be stored in
output_directory("./build/") # + Time.now.strftime('%F')+"/") ## store the ipa in this folder
archive_path("./archive/") # + Time.now.strftime('%F')+"/")
